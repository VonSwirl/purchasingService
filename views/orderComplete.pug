doctype html
html(lang="en")
  link(rel='stylesheet', href='/src/main/bootstrap/bootstrap.min.css', type='text/css')
  head
    include staff.html
    title Order Complete
  body
    h1 ORDER COMPLETE

    each item in items
        -var itemPrice = JSON.parse(item[2])
        p= item[1] + " ->  " + item[0] + "'s from " + itemPrice.name + "@ £" + itemPrice.price
    button(onclick = "goBack()") goBack


    script.
      var goBack = function(){
        window.location.href = '/purchasing'
      }